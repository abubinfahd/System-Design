# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/shortener
BASE_URL=http://localhost:8000

# Redis Configuration (Milestone 5: Caching)
REDIS_URL=redis://localhost:6379/0

# For Docker Compose development, these URLs will be handled automatically
# via environment variables in docker-compose.yml.
